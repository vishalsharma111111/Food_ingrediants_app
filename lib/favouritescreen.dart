import 'package:flutter/material.dart';

class FavouritScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('the favourite'),
       
    );
  }
}